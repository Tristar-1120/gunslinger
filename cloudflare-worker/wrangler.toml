name = "gunslinger-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Workers configuration
workers_dev = true

# Route configuration (optional - for custom domains)
# routes = [
#   { pattern = "game.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]
